# Filled button

Use for the single highest-emphasis action in a local action group.

```html
<button class="button button--filled" type="button">Next</button>
```

```css
.button--filled {
  background: var(--color-primary);
  color: var(--color-on-primary);
}
```
