# Filled tonal button

Use for an important action that should remain visually quieter than the filled
button.

```html
<button class="button button--tonal" type="button">Play</button>
```

```css
.button--tonal {
  background: var(--color-primary-container);
  color: var(--color-on-surface);
}
```
